<!-- Header with image -->
<div style="background: url(/images/travelbdg1.jpg)" class="page-holder bg-cover">
  <div class="container py-5">
    <header class="text-center text-white py-5">

      <h1 class="display-1 font-weight-bold mb-5">Destinations</h1>

      <p>Select the destination you want to travel to:</p>

      {{!-- For future development --}}
      <!-- Search -->
      {{!-- <div class="input-group">
      <input type="search" class="form-control rounded" placeholder="Search" aria-label="Search" aria-describedby="search-addon" />
         <button type="button" class="btn btn-outline-primary">search</button> <!-- NEED TO ADD QUERY TO SEARCH FROM -->
      </div> --}}

    </header>
  </div>
  <section class="container">
  
  <section class="flex-row justify-center">    
    {{#each destinations as |destination|}}
    {{> destination-details}}
    {{/each}}
  </section>
  <div>
    <p>
    </p>
  </div>
</section>
</div>