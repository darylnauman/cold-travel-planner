<!-- Header with image -->
<div style="background: url(/images/travelbdg1.jpg)" class="page-holder bg-cover">
  <div class="container py-5">
    <header class="text-center text-white py-5">

      <h1 class="display-1 font-weight-bold mb-5">Comments for {{location_name}}</h1>

      <p>{{destination.location_name}}1</p>
      <p>{{location_name}}2</p>
      <p>{{comment.location_name}}3</p>
      <p>{{comment.destination.location_name}}4</p>
      <p>{{comments.destination.location_name}}5</p>
      <p>{{comments.location_name}}6</p>
      <p>{{destination.id}}7</p>
      <p>{{comment.destination}}8</p>
      <p>{{destinations.location_name}}9</p>

    </header>
  </div>
  <section class="container">
  
  <section class="flex-row justify-center">    
    {{#each comments as |comment|}}
    {{> comments-details}}
    {{/each}}
  </section>
  <div>
    <p>
    </p>
  </div>
</section>
</div>

